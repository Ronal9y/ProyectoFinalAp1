@page "/"

<div class="pet-shop">
    <section class="section hero has-bg-image" id="home" aria-label="home" style="background-image: url('imagenes/hero-banner.jpg');">
        <div class="container">
            <h1 class="h1 hero-title font-weight-bold text-white" style="font-weight: bold; font-size: 4rem">
                Comida para mascotas de Alta Calidad
            </h1>
            <p class="hero-text" style="color:white">Oferta de hasta 40% de descuento hoy</p>
            @* <a href="#" class="btn" style="color:white, Background:black">Compra ahora</a> *@
        </div>
    </section>

    <section class="section category" aria-label="category">
        <div class="container">
            <h2 class="h2 section-title" style="font-size: 1.4rem">
                <span class="span" style="color:darkorange; font-size:1.5rem">Top</span> Categorias
            </h2>
            <ul class="has-scrollbar">
                <li class="scrollbar-item">
                    <div class="category-card">
                        <figure class="card-banner img-holder" style="--width: 330; --height: 300;">
                            <img src="imagenes/category-1.jpg" width="300" height="300" loading="lazy" alt="Cat Food" class="img-cover">
                        </figure>
                        <h3 class="h3">
                            <a href="#" class="card-title">Comida para gatos</a>
                        </h3>
                    </div>
                </li>
                <!-- Repite para otras categorías -->
            </ul>
        </div>
    </section>
</div>

<!-- Otras secciones como ofertas, productos, servicios, etc. -->
@code {
    // Aquí puedes agregar lógica de negocio si es necesario
}